[project]
name = "buspad_georef"
version = "0.1.0"
description = "Georeference YOLO bus-pad detections to EPSG:6539 map coordinates."
requires-python = ">=3.10"
dependencies = [
    "affine",
    "geopandas",
    "shapely",
]

[project.scripts]
buspad-georef = "buspad_georef.cli:main"